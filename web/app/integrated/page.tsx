export default function IntegratedPage() {
  return (
    <main>
      <h1>Integrated build</h1>
      <p>
        Two-column layout, structured output, source-quote highlighting,
        confirmed-actions list, telemetry panel. Lands in Phase 2.
      </p>
    </main>
  );
}
